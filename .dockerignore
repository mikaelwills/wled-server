# Rust build artifacts (but keep the release binary!)
target/debug/
target/*/incremental/
target/*/deps/
target/*/build/
**/*.rs.bk
*.pdb

# Frontend dependencies (but keep the build output!)
frontend/node_modules/
frontend/.svelte-kit/
frontend/.env
frontend/.env.local

# Git
.git/
.gitignore

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
/tmp/

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Scripts (except docker-start.sh which is needed)
restart.sh
deploy.sh

# Documentation
*.md
!README.md

# Claude
.claude/
CLAUDE.md
